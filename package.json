{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "beam",
  "description": "A monorepo of Raycast extensions.",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "eslint .",
    "fix-lint": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@raycast/eslint-config": "catalog:",
    "eslint": "catalog:",
    "prettier": "^3.5.3",
    "typescript": "catalog:"
  }
}
